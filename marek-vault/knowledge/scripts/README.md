---
type: index
tags: [scripts]
---

# Scripts Marek sent

One note per script, so I actually understand each one instead of letting them pile up in the DM.

Workflow when a new script arrives:

1. It lands in `attachments/` via the importer (linked from its archive message).
2. Copy `templates/script.md` into this folder, named after the script.
3. Fill in: what it does, which [[pipeline]] stage it belongs to, the notation it assumes, and a link
   back to the source message (`![[archive/YYYY-MM-DD#^msg-<id>]]`).

Keep the annotation here; don't paste the whole script into the committed note (it's in `attachments/`,
which stays local). Summarise and reference line numbers.

Nothing yet — the first import will populate `attachments/`.
