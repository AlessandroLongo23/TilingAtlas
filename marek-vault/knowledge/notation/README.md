---
type: index
tags: [notation]
---

# Notation

How Marek writes tilings down. The notes here decode the symbols so I can read his messages and his
code without guessing.

- [[vertex-configuration]] — the `3.3.4.3.4` cyclic-sequence notation for a vertex.
- [[half-edges-and-corners]] — the half-edge/corner labels the dual-search glues with (CLAUDE.md's
  "Conway symbols").
- [[palette-json-schema]] — the JSON that defines a tile set (the "alphabet").

See also [[glossary]] for one-liners, and the engine's own write-up at
[../../../tools/ctrnact-oracle/reference/algorithm.txt](../../../tools/ctrnact-oracle/reference/algorithm.txt).
