---
type: index
tags: [notation]
---

# Notation

How Marek writes tilings down, decoded from the chat so I can read his messages, his files, and his
code without guessing.

- [[conway-symbol]] — the gluing notation everything else orbits around. Start here.
- [[half-edges-and-corners]] — the labels the symbol is made of.
- [[vertex-configuration]] — configurations, combinations, and the F/A/R/S symmetry suffixes.
- [[tes-format]] — the render-ready HyperRogue format (`unittile`, `slider`, `conway`).
- [[solution-file-format]] — raw solver output, filenames, and the hybrid edge database.
- [[palette-json-schema]] — the repo port's tile-set JSON (this one is repo-side, not Marek's).

Hybrid symbols (`A3`, `B5`, `Aoo`) live with the theory in [[hybrid-identities]]. One-liners in the
[[glossary]]. Marek's own write-up:
[../../../tools/ctrnact-oracle/reference/algorithm.txt](../../../tools/ctrnact-oracle/reference/algorithm.txt).
