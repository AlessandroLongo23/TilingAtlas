# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — **build-vs-pivot call IN: build Delaney–Dress, probe-first.** Start **M0–M1** per `../resources/research/delaney-dress-implementation-contract-2026-06-08.md` (symbol core + constrained orderly generator + `DSYM_PROBE`), new module `lib/classes/algorithm/delaney/` behind `USE_DSYM`, one branch at `run-pipeline.ts:147`, fresh `feat/delaney-dress` off master. **Deliverable = ΣcandidateSymbols vs k (k=1..4) + k=1 ⊇ 11; flag-off byte-identical.** Curve (vs orbifold 183→3103→186190) gates M2/M3 + k≥4. Work order: SYNC 2026-06-08 TA→CC.
- **TA** — drafting **B2 (T4)** realization-lemma obligations (the Stage-2 soundness gate between verifier and home-run); then T1 (3-method arc) + Delaney–Dress chapter (T5). Reflection-lemma hardening held (moot for orbifold viability per the count verdict).
- **Alessandro** — **call made: D-D probe-first.** Commit the contract + board + SYNC work order; the M1 count then decides verifier-only vs k≥4 home-run. k≤3 certified stands via torus regardless.
