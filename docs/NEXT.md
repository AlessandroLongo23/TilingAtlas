# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — **TH-4 + TH-13 star tables LANDED on `feat/th4-th13-star-tables` @ `7813ce8` (NOTES §35, awaiting merge decision)** — d_max(envelope) = 9 exact (δ ≤ 18k, F ≤ 42k), TH-13 19/8/5 + single-variant rider; constants INPUT only, discharge is TA's. Next: CB-5 → CB-4 → CB-6; **OP-1→2→3 UNBLOCKED (TH-9 ✓)** — honour rem:orbitdedup's 3 constraints (exact orbit ID, blanket/coset rotations, orbit-aware CB-7 guard), then OP-9 re-measure.
- **TA** — **star lane UNBLOCKED: TH-4 constants + TH-13 scoping table delivered (NOTES §35, SYNC 2026-06-10)** — consume d_max = 9 in the restated Remark 3 (δ ≤ 18k) + cor:starbox(i) (F ≤ 42k; Fig-3(=1) stratum 6, dent-reg 5); TH-13 lemma attempt scoped to the 8 POINT-ONLY rows (single-variant case proven vacuous; 5 γ=11 variants provably Fig-4-absent). Remaining TA by value: TH-12 (G&S 1977), TH-5/6/7.
- **Alessandro (decision)** — **theorem-certified k=3 via D-D?** Sweep = δ≤34 ≈ 10¹²±1 nodes ≈ days on 8 cores with a parallelized generator (see `dd-size-bound-sharpened-2026-06-10.md`). If GO → CC: parallel subtree dispatch + δ≤28 calibration run + M2 realizer on survivors.
- **Alessandro** — one open call: TH-10 in-thesis vs future-work. (CB-9 push: done, AL-approved.)
