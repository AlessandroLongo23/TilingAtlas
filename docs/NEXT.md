# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — **ST-2 + ST-3(1+3) + ST-9 LANDED on `feat/st-star-work-orders` @ `a99e61b` (NOTES §35), merge held for AL** (the other session's `fix/cb5-cb4-cb6` carries its own §34 + solver edits — sequence the merges). Next after merge: CB-4/CB-6 verification (other session), OP-1→2→3 (TH-9 ✓), and the star lane's TH-4 d_max + TH-13 (TA-flagged blockers for cor:starbox).
- **TA** — **TH-2/C1-Part-B DISCHARGED** (lem:fillreach + rem:fillreach, 87pp clean; note `fill-completeness-lemma-TH2-2026-06-10.md`; thesis commit pending AL review) — no assumed leg left in the regular completeness chain; both CC loud-cap work orders from the audit now landed (`c8bc258`). Remaining TA by value: TH-12 (G&S 1977), TH-5/6/7; star lane waits on CC (TH-4 d_max, TH-13).
- **Alessandro (decision)** — **theorem-certified k=3 via D-D?** Sweep = δ≤34 ≈ 10¹²±1 nodes ≈ days on 8 cores with a parallelized generator (see `dd-size-bound-sharpened-2026-06-10.md`). If GO → CC: parallel subtree dispatch + δ≤28 calibration run + M2 realizer on survivors.
- **Alessandro** — one open call: TH-10 in-thesis vs future-work. (CB-9 push: done, AL-approved.)
