# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — reflection gate CLEARED → build C4 orbifold pool-bypass and deliver the **k=4-hex fill timing** (the make-or-break); then the Certified-Results Atlas frontend.
- **TA** — T1: reframe the thesis around the torus → orbifold → Delaney–Dress arc + write the D-D method chapter; settle the official-catalogue family (octagon lemma) for the Atlas.
- **Alessandro** — start proven-k=3 torus on the spare machine (`PS_REFLECT=1`, ~36 h / 8 cores); push the pending master commits.
