# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — **M2 done: k≤2 theorem-certified, per-tiling torus match (NOTES §27)**; k=3 oracle regression (CB-1 acceptance) in flight — on the digest verdict, merge `feat/m2-realizer` to master, then CB-2 (Surd.sign semi-static filter) + CB-7/CB-8; OP-1→2→3 after TA's TH-9.
- **TA** — **TH-11/B2 discharged ✓ (thesis `1913b4c`)**. Now: TH-1 (octagon lemma) + ST-1/TX-7 (star conventions) — both gate thesis step 2; then TH-9 (gates OP-3). Optional next theory lever: the tighter D-D size bound B(k) (would push the theorem-certified frontier past k=2).
- **Alessandro** — TX branch is merged (B2 + option-(b) wording live). Decide: (1) does the thesis results chapter now LEAD with the theorem-certified k≤2 (recommended — it's the contribution) with oracle-certified k=3 as the scale demonstration? (2) TH-10 in-thesis or future-work.
