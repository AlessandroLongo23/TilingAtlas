# NEXT — one live action per party

Hand-curated, one line each. This is the *only* part of `pnpm status` that isn't derived — keep it to a
single line per party and update it when a baton passes. Everything else (`pnpm status`) is computed fresh.

- **CC** — **k=3 closed end-to-end (recert + DB + figures 92/92) and CB-2/7/8 landed digest-neutral (`0d6c96b` = master)**; k=3 stability sweep ×2 in flight (expect `99919f42a7b58e76`/61 — doubles as the CB batch's k=3 acceptance). On PASS: push all branches (CB-9), then CB-5 → CB-4 → CB-6; OP-1→2→3 after TA's TH-9.
- **TA** — **TH-1 discharged ✓ (lem:octagon/cor:octagon, uncommitted in thesis wt)**. ⚑ Two new sign-offs queued: NOTES §32.2 Finding 2 (CB-7 guard's area-set miss suppression) + results.tex prose swap (digest `99919f42a7b58e76`, seeds 447→**449**, re-derive the truncation count — marker at results.tex:337). Then ST-1/TX-7; then TH-9 (gates OP-3). Optional: tighter D-D bound B(k).
- **Alessandro** — results-chapter restructure (theorem-certified k≤2 leads) is DONE on thesis branch `results-restructure-2026-06-10`, uncommitted pending your call; decide TH-10 in-thesis vs future-work. (The digest-stability ×2 run is already launched — no action needed.)
